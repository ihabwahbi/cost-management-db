# Azure PostgreSQL Connection String
# Use the same connection string as the main cost-management app
# Schema isolation is handled by drizzle.config.ts (schemaFilter: ['dev_v2'])

DATABASE_URL=postgresql://iwahbi:PASSWORD@cost-management-db.postgres.database.azure.com:5432/postgres?sslmode=require

# Replace PASSWORD with your actual database password
# Get connection details from: Azure Portal → Database → Settings → Connection strings
